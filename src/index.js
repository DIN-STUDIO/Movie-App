import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root')); 
// jsx의 첫번째 규칙 => 하나의 컴포넌트만 렌더링할 수 있다. <App />이 컴포넌트.